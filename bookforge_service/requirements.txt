# --- formatting engine ---
weasyprint==69.0
python-docx==1.2.0
pillow==12.1.1
pypdf==5.9.0

# --- web service ---
fastapi==0.136.3
uvicorn[standard]==0.49.0
python-multipart==0.0.32

# --- accounts/library persistence ---
# SQLite (stdlib) is used by default; psycopg2 is only needed when DATABASE_URL is set
# (e.g. a Render/Neon Postgres) so accounts + books survive without a persistent disk.
psycopg2-binary>=2.9.9

# --- optional: real AI chapter art (OpenAI gpt-image-1) ---
# only needed if you set IMAGE_PROVIDER=openai + OPENAI_API_KEY; the tool runs
# without it (emblem crests are the fallback)
openai>=1.40.0
